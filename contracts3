<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>All Pro Painting Plus - Professional Estimate</title>

<style>
:root { 
    --allpro-blue: #002D62; 
    --border-heavy: 2px solid var(--allpro-blue); 
}

body { 
    font-family: "Segoe UI", Arial, sans-serif;
    background-color: #555; margin: 0; padding: 0; padding-bottom: 120px; 
}

.contract-page { 
    background: white; width: 8.5in; min-height: 10.4in; margin: 20px auto; 
    padding: 0.4in; box-shadow: 0 0 15px rgba(0,0,0,0.5); box-sizing: border-box; 
}

/* Vertical Photo Layout */
.photo-page { page-break-before: always; }
.photo-stack { display: flex; flex-direction: column; gap: 20px; margin-top: 10px; }
.photo-container { 
    border: 1px solid var(--allpro-blue); 
    padding: 15px; 
    background: #fff; 
    display: flex; 
    flex-direction: column;
    page-break-inside: avoid; /* Keeps description and photo together */
}

.preview-img { 
    width: 100%; 
    max-height: 2.8in; /* Calculated to fit 3 per page exactly */
    object-fit: contain; 
    margin-top: 10px; 
    display: none; 
    border: 1px solid #eee;
}

/* Standard Header/Info Styles */
.top-header { display: grid; grid-template-columns: 1fr 1.2fr; border-bottom: var(--border-heavy); padding-bottom: 12px; margin-bottom: 12px; }
.brand-side h1 { margin: 0; color: var(--allpro-blue); font-size: 26px; }
.cert-side { text-align: right; }
.badge-row img { height: 50px; margin-left: 10px; }

.info-grid { display: grid; grid-template-columns: 1fr 1fr; border: var(--border-heavy); margin-bottom: 15px; }
.info-cell { border: 1px solid var(--allpro-blue); padding: 2px 6px; }
.label { font-size: 9px; font-weight: 800; text-transform: uppercase; color: var(--allpro-blue); display: block; }
input { border: none; font-size: 13px; width: 100%; outline: none; background: transparent; }

table { width: 100%; border-collapse: collapse; border: var(--border-heavy); margin-bottom: 10px; table-layout: fixed; }
th { border: 1px solid var(--allpro-blue); background: #f0f4f8; padding: 6px; font-size: 11px; }
td { border: 1px solid var(--allpro-blue); vertical-align: top; }
textarea { width: 100%; border: none; font-size: 12px; padding: 8px; box-sizing: border-box; resize: none; overflow: hidden; min-height: 35px; }

.totals-area { display: grid; grid-template-columns: 1.2fr 0.8fr; gap: 15px; margin-top: 10px; page-break-inside: avoid; }

@media print {
    @page { size: Letter; margin: 0.3in; }
    body { background: white; padding: 0; }
    .no-print, .app-footer, .btn-red { display: none !important; }
    .contract-page { margin: 0 auto; width: 100%; box-shadow: none; border: none !important; page-break-after: always; }
}

.app-footer { position: fixed; bottom: 0; width: 100%; background: #111; padding: 20px; display: flex; justify-content: center; gap: 20px; z-index: 1000; }
.btn { padding: 12px 25px; font-weight: bold; cursor: pointer; border: 2px solid white; border-radius: 4px; color: white; }
.btn-blue { background: var(--allpro-blue); }
.btn-green { background: #27ae60; }
.btn-red { color: #cc0000; cursor: pointer; border: 1px solid #cc0000; background: #fff; padding: 4px 8px; font-size: 10px; align-self: flex-start; margin-top: 10px; border-radius: 3px; }
</style>
</head>

<body>

<div class="contract-page">
    <div class="top-header">
        <div class="brand-side">
            <h1>ALL PRO PAINTING PLUS</h1>
            <p>Servicing The Capital Region & The North Country</p>
            <p>(518) 701-0308 | www.allpropaintingplus.com</p>
        </div>
        <div class="cert-side">
            <div class="badge-row">
                <img src="lead-safe-certified-firm.png" alt="EPA">
                <img src="original333.png" alt="OSHA">
            </div>
            <span class="label">Document Date</span>
            <input type="date" id="docDate" style="text-align:right;">
        </div>
    </div>

    <div style="border: 3px double var(--allpro-blue); text-align: center; padding: 6px; margin-bottom: 12px; font-weight: bold; color: var(--allpro-blue); font-size: 11px;">
        LICENSED / CERTIFIED / FULLY INSURED / 40 YEARS EXPERIENCE
    </div>

    <div class="info-grid">
        <div class="info-cell" style="grid-column: span 2;"><span class="label">Customer Name</span><input type="text"></div>
        <div class="info-cell" style="grid-column: span 2;"><span class="label">Project Address</span><input type="text"></div>
        <div class="info-cell"><span class="label">Primary Phone</span><input type="text"></div>
        <div class="info-cell"><span class="label">Start Date</span><input type="date"></div>
        <div class="info-cell"><span class="label">Email Address</span><input type="text"></div>
        <div class="info-cell"><span class="label">Est. Completion</span><input type="date"></div>
    </div>

    <table>
        <thead>
            <tr>
                <th style="width:25%;">Area</th>
                <th style="width:45%;">Work Description</th>
                <th style="width:15%;">Materials</th>
                <th style="width:15%;">Labor</th>
            </tr>
        </thead>
        <tbody id="rows"></tbody>
    </table>

    <div class="totals-area">
        <div style="border:var(--border-heavy); padding:8px;">
            <span class="label" style="font-size: 7px;">Signature & Acceptance: Authority to enter contract and agreement to pay In Full On Day Of Completion.</span>
            <canvas id="sig-canvas" style="width:100%; height:90px;"></canvas>
        </div>
        <div style="border:var(--border-heavy); padding:8px; text-align:right; color:var(--allpro-blue);">
            <div style="font-size:12px;">Material Total: <span id="mDisp">$0.00</span></div>
            <div style="font-size:12px;">Labor Total: <span id="lDisp">$0.00</span></div>
            <div style="font-size:10px;">Tax: <input type="number" id="tax" style="width:60px;" oninput="calcTotal()" value="0"></div>
            <div style="font-size:18px; font-weight:bold; border-top:2px solid var(--allpro-blue); margin-top:4px; padding-top:4px;">
                TOTAL: <span id="gDisp">$0.00</span>
                <p style="font-size:9px; margin:0;">Contractors Signature</p>
            </div>
        </div>
    </div>
</div>

<div id="photoPagesContainer"></div>

<div class="contract-page" style="page-break-before: always;">
    <h3 style="color:#002D62; border-bottom:2px solid #002D62; padding-bottom:6px; margin-top: 0;">CONTRACT TERMS</h3>
    <div style="font-size:10px; line-height:1.4; text-align: justify;">
        <p><strong>LEGAL DISCLOSURES:</strong> Pursuant to NYS GBL § 771(1)(h), you have a right to cancel this contract within three business days. Balance is due on completion. Materials remain property of contractor until paid in full.</p>
        <p><strong>SITE SAFETY:</strong> Worksite is restricted. Children, elderly, and those with disabilities are prohibited from the worksite for safety reasons per OSHA regulations.</p>
        <p><strong>PAYMENT:</strong> Due in full on day of completion via local check, cash, or wire. Payment signifies 100% satisfaction.</p>
    </div>
</div>

<div class="app-footer no-print">
    <button class="btn btn-blue" onclick="addRow()">+ Add Line</button>
    <button class="btn btn-blue" onclick="addNewPhoto()">+ Add Photo</button>
    <button class="btn btn-green" onclick="window.print()">Generate PDF</button>
</div>

<script>
document.getElementById('docDate').valueAsDate = new Date();
function autoExpand(el){ el.style.height="auto"; el.style.height=el.scrollHeight+"px"; }

function addRow(){ 
    const tr=document.createElement('tr'); 
    tr.innerHTML=`<td><textarea oninput="autoExpand(this)"></textarea></td><td><textarea oninput="autoExpand(this)"></textarea></td><td><input type="number" class="mat" oninput="calcTotal()" value="0"></td><td><input type="number" class="lab" oninput="calcTotal()" value="0"></td>`; 
    document.getElementById('rows').appendChild(tr); 
}
addRow();

function calcTotal(){
    let m=0,l=0;
    document.querySelectorAll('.mat').forEach(i=>m+=Number(i.value)||0);
    document.querySelectorAll('.lab').forEach(i=>l+=Number(i.value)||0);
    let t=Number(document.getElementById('tax').value)||0;
    document.getElementById('mDisp').innerText='$'+m.toFixed(2); 
    document.getElementById('lDisp').innerText='$'+l.toFixed(2); 
    document.getElementById('gDisp').innerText='$'+(m+l+t).toFixed(2);
}

let photoCount = 0;
function addNewPhoto() {
    photoCount++;
    const container = document.getElementById('photoPagesContainer');
    let pages = container.querySelectorAll('.photo-page');
    let lastPage = pages[pages.length - 1];

    // Create a new photo page if none exist or if the last page has 3 photos
    if (!lastPage || lastPage.querySelector('.photo-stack').children.length >= 3) {
        lastPage = document.createElement('div');
        lastPage.className = 'contract-page photo-page';
        lastPage.innerHTML = `<h2 style="border-bottom:var(--border-heavy);color:var(--allpro-blue); margin-top:0;">PROJECT PHOTO LOG</h2><div class="photo-stack"></div>`;
        container.appendChild(lastPage);
    }

    const photoId = 'img-' + photoCount;
    const photoDiv = document.createElement('div');
    photoDiv.className = 'photo-container';
    photoDiv.innerHTML = `
        <span class="label">Photo Description</span>
        <input type="text" placeholder="e.g., Kitchen Cabinets - Before">
        <input type="file" accept="image/*" onchange="pv(this,'${photoId}')" class="no-print" style="margin-top:10px;">
        <img id="${photoId}" class="preview-img">
        <button class="btn-red no-print" onclick="removePhoto(this)">Delete Photo</button>
    `;
    lastPage.querySelector('.photo-stack').appendChild(photoDiv);
}

function removePhoto(btn) {
    const stack = btn.closest('.photo-stack');
    const page = btn.closest('.photo-page');
    btn.parentElement.remove();
    // Remove the page if it's empty
    if (stack.children.length === 0) { page.remove(); }
}

function pv(input,id){ 
    if(input.files && input.files[0]){ 
        let r=new FileReader(); 
        r.onload=e=>{ let img=document.getElementById(id); img.src=e.target.result; img.style.display='block'; }; 
        r.readAsDataURL(input.files[0]); 
    } 
}

// Canvas Setup
const canvas=document.getElementById('sig-canvas'); const ctx=canvas.getContext('2d');
function resizeCanvas(){ const ratio=window.devicePixelRatio||1; canvas.width=canvas.offsetWidth*ratio; canvas.height=canvas.offsetHeight*ratio; ctx.scale(ratio,ratio); ctx.lineWidth=2; }
resizeCanvas(); window.addEventListener('resize',resizeCanvas);

let drawing=false;
function pos(e){ 
    const r=canvas.getBoundingClientRect(); 
    const cx = e.touches ? e.touches[0].clientX : e.clientX;
    const cy = e.touches ? e.touches[0].clientY : e.clientY;
    return{ x: cx - r.left, y: cy - r.top }; 
}
canvas.addEventListener('mousedown',e=>{ drawing=true; ctx.beginPath(); let p=pos(e); ctx.moveTo(p.x,p.y); });
canvas.addEventListener('mousemove',e=>{ if(!drawing)return; let p=pos(e); ctx.lineTo(p.x,p.y); ctx.stroke(); });
window.addEventListener('mouseup',()=>drawing=false);
canvas.addEventListener('touchstart',e=>{ drawing=true; ctx.beginPath(); let p=pos(e); ctx.moveTo(p.x,p.y); },{passive:false});
canvas.addEventListener('touchmove',e=>{ if(!drawing)return; e.preventDefault(); let p=pos(e); ctx.lineTo(p.x,p.y); ctx.stroke(); },{passive:false});
</script>
</body>
</html>
